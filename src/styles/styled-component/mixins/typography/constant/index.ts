import { Property } from 'csstype';

export const DEFAULT_FONT_WEIGHT: Property.FontWeight = 400;

export const DEFAULT_TEXT_TRANSFORM: Property.TextTransform = `initial`;

export const DEFAULT_FONT_STYLE: Property.FontStyle = `initial`;

export const DEFAULT_TEXT_DECORATION: Property.TextDecoration = `none`;
